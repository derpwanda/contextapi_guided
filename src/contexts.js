import { createContext } from "react";

//creates a namespace area for our context to live
//you CAN pass value to context, you will still need a provider
export const ThemeContext = createContext({});
